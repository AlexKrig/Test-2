import sun.util.BuddhistCalendar;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.HashMap;

//public class HeshTest {
    //public static void main(String[] args) {
       // HashMap<Integer, Person> list=new HashMap<>();
       // for (int i;i<2;i++){
          //  BufferedReader buf = new BufferedReader(new InputStreamReader(System.in));
           // try{
              //  System.out.println(" ");
               // String log =buf.readLine();
               // String pass = buf.readLine();
                //int number = Integer.valueOf(buf.readLine());
              //  int age=Integer.valueOf(buf.readLine());
             //   list.put(number, new Person(log,pass,age));
           // } catch (IOException e) {
         //       e.printStackTrace();
         //   }
      //  }

   /// }
//}
